import "package:flutter/material.dart";


class UsWalletOrders extends StatelessWidget {
  const UsWalletOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("Transaction History"),

      ],
    );
  }
}
